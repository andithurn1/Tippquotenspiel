import Einstellungen from "@/components/Einstellungen";

export const metadata = { title: "Einstellungen · Tippquotenspiel" };

export default function EinstellungenPage() {
  return <Einstellungen />;
}
