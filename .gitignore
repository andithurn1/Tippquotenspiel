
# Abhängigkeiten
# Delta-SQL ist eine Wegwerf-Datei (npm run seed:delta) — nach dem
# Lokale Env-Dateien (API-Keys gehören NIE ins Repo)
# Next.js Build
# Persönliche Claude-Code-Einstellungen (nicht teamweit)
# Produktion
# Sonstiges
# Vercel
# naechsten odds:holen veraltet und dann gefaehrlich. Nie einchecken.
*.pem
.DS_Store
.api-waehrend-app-build/
.claude/launch.json
.claude/settings.local.json
.env
.env*.local
.vercel
/.next
/build
/node_modules
/out
npm-debug.log*
out/
scratch/
supabase/_quoten-update.sql

# 🔴 Auch SICHERUNGEN der Umgebungsdatei. Am 29.08.2026 lag ein
# `.env.local.bak` als unverfolgte Datei da — mit den Supabase-Schlüsseln
# darin. Ein `git add -A` hätte sie committet, und Schlüssel aus der
# Historie zu entfernen ist ungleich aufwendiger, als sie gar nicht
# hineinzulassen.
.env.local.*
.env.*.bak
