# Abhängigkeiten
/node_modules

# Next.js Build
/.next
/out

# Produktion
/build

# Sonstiges
.DS_Store
*.pem
npm-debug.log*

# Lokale Env-Dateien (API-Keys gehören NIE ins Repo)
.env
.env*.local

# Vercel
.vercel

# Persönliche Claude-Code-Einstellungen (nicht teamweit)
.claude/settings.local.json
.claude/launch.json
scratch/

# Delta-SQL ist eine Wegwerf-Datei (npm run seed:delta) — nach dem
# naechsten odds:holen veraltet und dann gefaehrlich. Nie einchecken.
supabase/_quoten-update.sql
